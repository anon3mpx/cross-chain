Here’s the non-DB, non-API summary of today’s updates:

  1. Foundry + Solidity workflow

  - Kept project on Foundry.
  - Added/validated Solidity build+test flow (sol:build, sol:test).
  - Solidity suites are passing (11/11).

  2. New rail contracts

  - Added Axelar rail plugin and receiver adapter:
      - src/contracts/rails/AxelarRailPlugin.sol
      - src/contracts/rails/AxelarReceiverAdapter.sol
  - Added LayerZero rail plugin and receiver adapter:
      - src/contracts/rails/LayerZeroRailPlugin.sol
      - src/contracts/rails/LayerZeroReceiverAdapter.sol

  3. Intent/Router contract data model updates

  - Extended intent payload support for native-dst and THOR metadata:
      - nativeDstAddress, thorAssetIdentifier, minThorOutput
  - Router now forwards these into bridge params.

  4. Empseal aggregator integration hardening

  - Verified/updated IEmpsealRouter ABI alignment with reference behavior.
  - Fixed EmpsealSwapPlugin implementation:
      - full ISwapPlugin compatibility
      - correct token pull + approve flow
      - stronger trade/path validation
      - interface support cleanup
  - Added dedicated tests for this plugin.

  5. Routing/chain core logic fixes (VPS services/config)

  - Fixed route-builder hub-hop issues:
      - correct second-leg settlement token handling
      - avoid duplicate same-rail combos when alternatives exist
      - better precomputed scoring paths
      - viability based on hub aggregator presence for token conversion
  - Added missing stuck-timeout coverage for additional rails.

  6. Quote engine core wiring (service layer)

  - Quote engine now uses real chain/route builder flow and route-type logic.
  - Handles aggregator-present/absent behavior per source/destination side.
  - Uses chain-specific settlement token and swap plugin config inputs.
  - Better fee calc and cache key shape.
  - Generates on-chain-style bytes32 intent IDs.
  - Gracefully returns no-route when swap quote adapters are unavailable.

  7. Rail plugin ID correctness

  - Replaced placeholder plugin ID encoding with actual keccak-style bytes32 IDs to match on-chain registry expectations.

  8. Documentation artifact

  - Added full end-to-end system Mermaid diagrams:
      - E2E_MERMAID_DIAGRAMS.md


› now provide db and api updates separately


• DB updates

  1. Added production Postgres schema scaffold (7-table model) with indices, constraints, and triggers:

  - src/vps/db/schema.sql

  2. Added DB connection helper (env-driven pool config):

  - src/vps/db/postgres.ts

  3. Added BigInt-safe JSON serialization/revival helpers for intent/quote persistence:

  - src/vps/db/json.ts

  4. Added intent repository with:

  - snapshot upsert
  - event append
  - intent lookup/by-status queries
  - src/vps/db/IntentRepository.ts

  5. Added persistence adapter to hook DB writes to existing IntentEngine.onStateChange (no full refactor required):

  - src/vps/db/IntentPersistence.ts

  6. Added bootstrap helper for easy attachment:

  - src/vps/db/bootstrap.ts
  - barrel export: src/vps/db/index.ts

  7. Added DB integration doc for self-hosted rollout:

  - docs/postgres-selfhost-integration.md

  8. Updated env/sample + script:

  - .env.example (PG vars section)
  - package.json script db:migrate was added earlier in that DB pass

  Note: later in your branch, the src/vps/db/* files are currently absent from working tree state; this list reflects
  what was created during the DB update pass.

  ———

  API updates

  1. Added request/response codec utilities:

  - parse quote request safely (amountIn -> bigint)
  - bigint-safe serialization for quote payloads
  - src/vps/api/quoteCodec.ts

  2. Updated PartnerAPI:

  - made /partner/register public before auth middleware
  - fixed quote parsing/validation flow
  - fixed rate-limit header handling
  - switched non-quote endpoints/webhook paths to non-mutating key validation
  - moved to shared integration builder for tx payload response
  - file: src/vps/api/PartnerAPI.ts

  3. Updated StatusAPI:

  - uses shared quote parsing/validation
  - returns serialized quote
  - now also returns integration payload for internal dapp tx execution
  - file: src/vps/api/StatusAPI.ts

  4. Added unified shared calldata/integration builder service:

  - src/vps/services/IntentCalldataBuilder.ts
  - both Status/Partner quote responses now rely on same RouterV1 calldata encoding path.

  5. Added ethers dependency to support ABI encoding and address/bytes validation used by shared builder:

  - package.json / package-lock.json